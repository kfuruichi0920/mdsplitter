/**
 * @file sum.ts
 * @brief 数値の合計を計算する関数。
 * @details
 * 配列または2値の加算を行います。@todo 境界条件・例外処理の明確化。
 * @author K.Furuichi
 * @date 2025-11-02
 * @version 0.1
 * @copyright MIT
 */

/**
 * @brief 2つの数値の合計を返す。
 * @param a 1つ目の数値。
 * @param b 2つ目の数値。
 * @return 合計値。
 * @throws なし
 */
export function sum(a: number, b: number): number {
    return a + b;
}